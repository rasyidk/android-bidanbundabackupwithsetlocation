<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.bidanbunda2">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".login.login" /> <!-- <activity android:name=".MainActivity" /> &lt;!&ndash; <activity android:name=".bottomnavigation.videomateri.hehe" /> &ndash;&gt; -->
        <!-- <activity android:name=".bottomnavigation.videomateri.list_videomateri.hehe" /> -->
        <activity android:name=".Splashscreen" />
        <activity android:name=".MainActivity" />
        <activity android:name=".signup.signUp" />
        <activity
            android:name=".bottomnavigation.videomateri.list_videomateri.play_lvm.playlvm"
            android:configChanges="orientation|screenSize|keyboardHidden|smallestScreenSize|screenLayout" />
        <activity android:name=".bottomnavigation.videomateri.list_videomateri.list_videomateri" />
        <activity android:name=".daftarPuskesmas.daftarPuskesmas">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>